const { login } = require('./lib/admin-auth');
exports.handler=async(event)=>{if(event.httpMethod!=='POST')return{statusCode:405,body:'Method Not Allowed'};try{const {username,password}=JSON.parse(event.body||'{}');if(!username||!password)return{statusCode:400,body:JSON.stringify({ok:false,message:'اسم المستخدم وكلمة المرور مطلوبان'})};const token=await login(username,password);return{statusCode:200,headers:{'Content-Type':'application/json'},body:JSON.stringify({ok:true,token})}}catch(err){const code=err.message==='INVALID_LOGIN'?401:500;return{statusCode:code,headers:{'Content-Type':'application/json'},body:JSON.stringify({ok:false,message:err.message==='INVALID_LOGIN'?'بيانات الدخول غير صحيحة':'تعذر إنشاء جلسة آمنة'})}}};
