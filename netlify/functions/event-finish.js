const { requireAdmin } = require('./lib/admin-auth');
const { getFirestore, FieldValue } = require('./lib/firestore-admin');
exports.handler=async(event)=>{if(event.httpMethod!=='POST')return{statusCode:405,body:'Method Not Allowed'};try{await requireAdmin(event);let payload;try{payload=JSON.parse(event.body||'{}')}catch{return{statusCode:400,body:JSON.stringify({ok:false,message:'بيانات غير صالحة'})}};const{eventCode,status}=payload;if(!eventCode||!['finished','active'].includes(status))return{statusCode:400,body:JSON.stringify({ok:false,message:'بيانات ناقصة'})};const db=getFirestore();const ref=db.collection('events').doc(String(eventCode));if(!(await ref.get()).exists)return{statusCode:404,body:JSON.stringify({ok:false,message:'المناسبة غير مسجّلة'})};await ref.set({status,finishedAt:status==='finished'?FieldValue.serverTimestamp():null,updatedAt:FieldValue.serverTimestamp()},{merge:true});return{statusCode:200,body:JSON.stringify({ok:true})}}catch(err){console.error('event-finish',err);return{statusCode:err.message?.startsWith('AUTH_')?401:500,body:JSON.stringify({ok:false,message:err.message||'تعذر تحديث المناسبة'})}}};
